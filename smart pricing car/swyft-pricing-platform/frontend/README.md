# Admin Frontend (Vite)

```bash
npm install
npm run dev
```

Proxies `/api/*` to `http://127.0.0.1:8000` with path rewrite. Configure `ADMIN_UI_JWT_SECRET` / `ADMIN_UI_PASSWORD` on the API gateway before using login.
